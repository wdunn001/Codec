{
  "name": "@codecai/mcp-leaf-example-time",
  "version": "0.1.0",
  "private": true,
  "description": "Reference Codec-aware MCP server. Demonstrates leaf-mode tokenization via @codecai/mcp-leaf — a downstream Codec-aware gateway (metamcp at feat/codec-binary-transport) detects the pre-tokenized tool result and skips its back-compat shim.",
  "type": "module",
  "bin": {
    "codec-time-leaf": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && node -e \"require('fs').chmodSync('dist/index.js', 0o755)\"",
    "start": "node dist/index.js",
    "smoke": "node --import tsx test/smoke.test.ts"
  },
  "dependencies": {
    "@codecai/mcp-leaf": "^0.1.0",
    "@codecai/web": "^0.3.0",
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=18"
  }
}
