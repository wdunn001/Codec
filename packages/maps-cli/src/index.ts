/**
 * @codecai/maps-cli: programmatic API.
 *
 * Use this when integrating dialect map generation into a build pipeline.
 * For one-off generation use the `codecai-maps` CLI.
 */
export {
  convertHFTokenizer,
  fetchAndConvert,
  hashMap,
  type HFTokenizerJson,
  type ConvertOptions,
  type FetchAndConvertOptions,
} from './convert.js';
