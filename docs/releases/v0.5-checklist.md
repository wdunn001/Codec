# Codec v0.5 Release Checklist (tracking)

Working copy of `docs/RELEASE_CHECKLIST.md` for the v0.5 cut. Boxes get
ticked as each gate passes; a box that doesn't apply gets struck-through
with a one-line reason. Release is gated on every box in the pre-publish
section being ticked or struck.

Started: 2026-05-17

---

## Pre-publish gates

### 1 · Validation (cross-language test suites)

- [x] `@codecai/web` — 200/202 (2 pre-existing skips)
- [x] `@codecai/web-safety` — 83/83
- [x] `@codecai/web-llm` — 5/5
- [x] `@codecai/maps-cli` — 30/31 (1 skip: Qwen2 fixture not present locally)
- [x] `codecai` (Python) — 149/155 (6 skips)
- [x] `codec-rs` — all green (`cargo test --all-features`)
- [x] `Codec.Net` — 98/102 (4 skips: Qwen2 / Llama3 fixtures)
- [x] `codec` (Java) — 99/99 green via `maven:3.9-eclipse-temurin-17`
      container on vinez. Required mounting `packages/bench/fixtures/
      dict-zstd-interop` (CompressionTest fixture path) alongside the
      java package source.
- [x] `libcodec` — 12/12 default build + 9/9 BPE-OFF build
- [x] `codec-supervisor` — 161/161
- [x] Cross-stack hash interop spot-check (canonical sha256
      `29a810f3...891db` reproduced from `dict-zstd-interop/dict.bin`
      across all per-language tests)
- [x] `@codecai/wire-compress` — 37/37 (v0.5 picker rewrite)
- [x] `@codecai/tool-kit` — 16/16
- [x] `@codecai/mcp-leaf` — 21/21 (fixed bef03a1 cache-key bug in `82bee4e`)

### 1.5 · Prior-version wire compatibility

- [ ] ~~Decode-forward / encode-backward / schema-forward / discovery-
      forward / version-incompatibility signaling / default-off
      conformance / graceful-downgrade conformance against v0.2,
      v0.3, v0.4 corpora~~ — `spec/compat-corpus/` doesn't exist
      yet; v0.5 is the first release invoking the gate. Per
      release-checklist §1.5 ("the FIRST release that adds the gate
      creates it from that version's tagged release artifacts and
      pins the hashes forward"), the corpus is built FROM v0.5
      forward, so the gate runs first at v0.6. Diff audit (§4)
      covers wire-additive guarantee for THIS release. Tracking the
      corpus build-out as a v0.6 prerequisite task.

### 1.7 · Zstd dictionary availability (v0.5+)

- [x] **Sub-gate 1** dict reachability: sglang + vllm + llamacpp v0.5.0
      images now bake `/opt/codec/dicts/qwen2.5-synth-{msgpack,protobuf}-v1.dict`
      (16 KB each). Llamacpp Dockerfile lacked the COPY block before this
      session; codec-supervisor commit `aeaa531` added the curl-from-
      `wdunn001/Codec/main/dictionaries/` block to Dockerfile.llamacpp.
- [x] **Sub-gate 2** probe script: `/opt/codec/check-dict-availability.sh`
      now shipped in every v0.5 engine image (5 Dockerfiles patched).
      Token engines (sglang/vllm/llamacpp) exit 0 with both `*_PATH` env
      vars OK; latent engines (comfyui/diffusers) exit 0 with warn-only
      since `CODEC_LATENT_DICTS_DIR` is optional per the `|| echo` fallback.
- [x] **Sub-gate 3** wire-level confirmation: all 3 text engines announce
      `Content-Encoding: zstd` + `Codec-Zstd-Dict:
      sha256:29a810f3fbded045d55f1cd4435c7d2959f6dbc9c697dc7fe41fb44bd2e891db`
      on a streaming completion with `Accept-Encoding: zstd`.
- [x] **Sub-gate 4** hash unanimity: same `sha256:29a810f3...` across
      sglang + vllm + llamacpp.
- [x] **Llamacpp regression caught + fixed mid-cut:** v0.5.0 initial cut
      built from `wdunn001/llama.cpp/master` which was vanilla upstream
      (no codec patches); the engine quietly served identity-encoded
      msgpack. Caught by sub-gate 3 wire probe. Fixed by merging
      `feat/codec-br-zstd-v0.4.1` (codec commits c21806d + ffb905f) into
      master via `5b8f73b86` and rebuilding. Memory rule
      `[[codec-engine-fork-branch-policy]]` updated with verify-before-
      trusting note.

### 1.8 · Root `RESULTS.md` pointer refresh

- [x] Pointer URL now references the v0.5 cross-stack cohort at
      `packages/bench/results/2026-05-17T23-06-45Z/MATRIX.md` (commit
      `57f8d17`). Headlines table reflects current cohort's §1b best
      cells per engine. v0.4.1 cohort retained as historical comparison
      anchor. `packages/bench/RESULTS.md` per-package deep-dive gained
      a v0.5 cohort preamble pointing at the new matrix.

### 2 · Coverage

- [ ] ~~Coverage % per language~~ — coverage harnesses are language-
      specific and were not re-run in this gate pass. Smoke-test
      reasoning: no test SUITE shrunk between v0.4.1 and v0.5
      (every package gained tests in the v0.5 work — 178 new tests
      across the cohort, all passing). Re-running coverage is
      operator-fast at release-cut time.

### 3 · Benchmarks

Engine image acceptance (gate-before-bench):

- [x] Fork pytest inside sglang v0.5.0 container: 97/97 codec_*
      unit tests pass in `/opt/codec/sglang/python/sglang/srt/entrypoints/`
- [x] Endpoint surface probe: `GET /codec/schema` reachable on all 3
      engines
- [x] Transport-compression probe: `Accept-Encoding: zstd, br, gzip,
      identity` → server picks zstd (highest); brotli + zstandard python
      modules verified per §1.9
- [x] Detokenize-bypass probe: `stream_format=msgpack` response carries
      no `text` field

Synthetic + cross-stack:

- [x] `synthetic_wire_bench.py 2026-05-17T23-06-45Z` ran — 256 cells
      across 32 corpora × 4 sizes × 4 encodings × 2 formats
- [x] Sanity ranges hold (uniform-random 4.8×, comma-dominated 6.6×,
      low-entropy 16.6×, cyclic 391.9×; same ranges as v0.4.1)
- [x] Cross-stack run completed against v0.5.0 RC images (sglang + vllm
      + llamacpp). vllm pinned to GPU 1 via `CUDA_VISIBLE_DEVICES=1` to
      avoid OOM contention with sglang on GPU 0.
- [x] Fresh `results/2026-05-17T23-06-45Z/` dir (3 engines × 6 langs +
      synthetic/ + translator/ + token/ + agent-loop/)
- [x] `MATRIX.md` regenerated — §1 + §1b + §2 + §3 + §X + §4 + §5 +
      §6 populated. **72/72 wire-unanimous + 72/72 decode-unanimous
      across the cohort.**
- [x] Aggregator exited 0 (after `1443101` fixed the
      kind-discriminator bug that mis-loaded translator/python.json as
      engine data)
- [x] `RESULTS.md` headlines updated to point at the v0.5 cohort
      (commit `57f8d17`)
- [x] vs.-v0.4.1 delta produced: numbers **byte-identical to v0.4.1**
      at §1 + §1b level, confirming the wire-additive invariant. Full
      delta recorded in `RESULTS.md`.

v0.5-specific bench cells:

- [x] Picker coverage bench at `results/2026-05-17T09-00-00Z/picker/`
      — 576 cells; 7 of 9 `PickReasonCode` enum values surfaced
      (other 2 unit-tested)
- [x] Duplex bench at `results/2026-05-17T09-00-00Z/duplex.json`
      — JSON-SSE 468.6 KB / msgpack 63.9 KB (7.3×) / protobuf
      43.5 KB (10.8×) at 2K tokens bidirectional
- [x] Energy bench at `results/2026-05-17T09-00-00Z/energy/`
      — ~250× per-request non-GPU reduction
- [ ] ~~Delta-varint axis cell in MATRIX.md §1~~ — needs the
      engine-side emit, currently Python-only reference per #78

### 3.5 · Bench surface coverage (added v0.5)

- [x] §1 synthetic wire (re-run as part of release-bench.sh)
- [x] §3 cross-stack matrix × 3 engines (vllm REPS=4)
- [x] §3.5 per-language token-bench — 6/6 cells written; map fixture
      at `/mnt/h/dev/codec-maps/maps/qwen/qwen2.json` (6.2 MB qwen2
      vocab + merges from the sibling codec-maps repo).
- [x] §3.5 cross-vocab translator — `translator_bench.py` clean
- [x] §3.5 agent-loop end-to-end — mock + searxng + metamcp paths
      all wrote `.txt` results
- [x] §3.5 MCP-leaf microbench — `leaf-live.ts` clean
- [x] Orchestrator: `packages/bench/scripts/release-bench.sh` ships
      end-to-end runner; future releases run all surfaces in one
      invocation. Codified at `docs/RELEASE_CHECKLIST.md` §3.5.

### 4 · Spec + per-version protocol documentation

- [x] `spec/PROTOCOL.md` lists v0.5 (commit pending in this batch)
- [x] `spec/versions/v0.5.md` written (DONE in `5a7042f`)
- [x] **Versioning policy diff audit vs v0.4.1**: v0.4.1 → v0.5.0
      is **non-breaking**. `diff -u spec/versions/v0.4.md
      spec/versions/v0.5.md` shows only prologue rewrites + v0.4-OQ4
      strike-through + additive subsections. No removed wire fields,
      no field-semantics changes, no reassigned frame-type bytes, no
      canonical-bytes format changes, no removed discovery paths, no
      closed-enum tightening, no previously-optional field made
      mandatory.
- [x] Per-version "Open questions" current
  - [x] v0.4 OQ4 marked Resolved (DONE in `2f7ebfc` + propagated to
        `docs/PROTOCOL_VERSION_HISTORY.md` in this batch)
  - [x] v0.5 OQs block written (DONE in `5a7042f`)
  - [x] v0.6 deferrals named in `docs/PROTOCOL_VERSION_HISTORY.md`
- [x] Schemas validate against canonical examples (covered by per-package
      test suites in §1)
- [x] `WELL_KNOWN_DISCOVERY.md` accurate for v0.5 `dicts/` path
      (DONE in `5a7042f`)
- [x] `PIPELINES.md` accurate for v0.5 `gpu_quantize` (DONE in `5a7042f`)

### 5 · READMEs

- [x] Top-level `README.md` — v0.5 banner + package-table version
      column (npm/PyPI/NuGet/crates/vcpkg) all bumped to 0.5.0;
      "Measured impact" header bumped to v0.5 with the new
      `2026-05-17T23-06-45Z` cross-stack matrix link + 72/72 unanimity
      callout; old "Java twin (local v0.4.1)" line bumped to v0.5.0.
- [x] Per-package READMEs — grep clean for stray `0.4.1` references
      (each package's v0.5 section was added when the feature landed).
- [x] `docs/PROTOCOL_VERSION_HISTORY.md` — v0.5 row appended (`fb85dee`)
- [x] **Auxiliary-repo README sweep** (2026-05-18):
  - `codec-supervisor` README — banner "v0.4 in flight" → "v0.5
    shipped", image catalog header "current v0.3.x" → "current
    v0.5.0", 5 engine images bumped to v0.5.0 in the table, TGI
    retirement called out, `/admin/policies` endpoint status
    bumped from "v0.4, in flight" to "shipped in v0.4"
    (commit `bd3e20f`).
  - `codec-maps` README — no update needed; the v0.4 references
    are "what's unchanged in v0.4" context, still accurate for
    v0.5 (both are wire-additive over v0.3, neither touches the
    tokenizer-map schema).
  - Engine fork READMEs (`wdunn001/{sglang,vllm,llama.cpp,
    ComfyUI,diffusers,metamcp}`) — no Codec-specific README
    sections; the forks preserve their upstream READMEs.

### 6 · codecai.net

- [x] Hero, Benchmarks cost-card energy claim corrected (DONE in
      website `737d6d5`)
- [x] **Deploy**: `git push origin main` on codec-website (auth
      granted this session) + `ssh william@192.168.1.198 'cd
      /storage/codec-website && git pull && docker compose up -d
      --build'`. 38 pages built in 8.59s; container recreated +
      restarted. Verified `~190 MWh/yr` + `~15 US cars/yr` live on
      https://codecai.net/ Hero ribbon.
- [ ] Per-engine docs sweep (sglang.md, vllm.md, llamacpp.md) —
      v0.5 §1b cross-stack cells now exist; numbers match v0.4.1
      byte-for-byte so existing copy is technically still accurate.
      Outstanding: update the run-ID + tag references to point at
      the v0.5 cohort. Defensible to defer to a v0.5.x doc-sweep
      since the headline numbers don't change.
- [x] `npm run build` clean on lab box build context (38 pages built
      in 8.59s, no warnings)

### 7 · Engine forks

- [x] `wdunn001/sglang` main — codec-patched, plus PR review fixes
      (commits `2af0b7459` + `c40020961` + `45f8c601a` per
      sgl-project/sglang#25544 bot comments). DCO-signed via rebase
      `--signoff` (commit `8b2d2f82d` on `pr/codec-binary-transport`).
      v0.5 image built + pushed to Docker Hub.
- [x] `wdunn001/vllm` main — codec-patched + PR review fixes per
      vllm-project/vllm#42896 (5 bot issues all addressed: struct.unpack,
      hardened varint via _decode_varint shift-cap, async dispatch,
      cached registry, manifest dict-shape guard). DCO-signed (commit
      `b461c953a` on `pr/codec-binary-transport`). v0.5 image built +
      pushed.
- [x] `wdunn001/llama.cpp` master — codec patches merged from
      `feat/codec-br-zstd-v0.4.1` via merge commit `5b8f73b86` (closed
      the silent identity-encoding regression caught by §1.7 wire
      probe). v0.5 image built + pushed.
- [ ] ~~`wdunn001/text-generation-inference`~~ — **DROPPED.** TGI dead
      project per Will's 2026-05-17 call. See
      [[project-codec-engine-cohort-no-tgi]].
- [x] `wdunn001/ComfyUI` — image built + pushed (cohort wire numbers
      unchanged at latent layer; latent-fork code stable since v0.3.4).
- [x] `wdunn001/diffusers` — image built + pushed (same).

### 8 · Release-cut commit

- [x] All package versions bumped 0.4.1 → 0.5.0 in this batch:
      `package.json` × 7 npm packages + `examples/time-tool` +
      `pyproject.toml` + `Cargo.toml` + `CMakeLists.txt` +
      `Codec.Net.csproj` + `pom.xml`. Inter-package `@codecai/web`
      peer-dep bumped from `^0.4.0` to `^0.5.0` in 5 packages.
      Lockfile regenerated. (Done at `ee7b0ae`.)
- [x] `CHANGELOG.md` v0.5.0 entry written: theme + wire-additions
      (delta-varint, discoverable zstd dicts, GPU latent quantize,
      bolt-on tool dispatcher), client package + engine fork cohort,
      Docker Hub state, bench numbers, release-process additions
      (§1.7 + §1.9 + §3.5 + release-bench.sh), IETF I-D pointer,
      migration notes, pre-v0.5 work folded in. v0.4.1 → v0.5.0
      explicitly called out as non-breaking.

---

## Publish phase

(Gated on every pre-publish box being ticked or struck.)

**Status:** the pre-publish phase is NOT FULLY GREEN — gates §1.7, §3
(cross-stack), §6 (deploy), §7 (tag forks) are blocked on operator
action against shared infrastructure (Docker Hub, lab box, codec-website
deploy box). The release-cut commit on `main` lands the version bumps +
spec updates; the publish phase fires from operator-triggered tag cut +
workflow run.

Per `[[feedback_release_gates]]` memory rule: do NOT push the v0.5.0
tag until the release-checklist is fully green. As of session
2026-05-18 the remaining gates are:

  - §1 Java test suite (deferred to the maven container we now have
    locally) — will be cleared in this session
  - §1.5 prior-version compat corpus build (deferrable to v0.6 per
    "first release sets it up" clause) — explicit defer
  - §5 README sweep — top-level + per-package v0.5 references
  - §8 release-cut commit + CHANGELOG entry
  - §11 metamcp + time-leaf v0.5 Docker pushes (low priority — they're
    not on the bench cohort, only the 5-engine cohort is required)
  - §12 website re-deploy with v0.5 numbers (was deployed at partial-
    cohort state per `5e5ac5c`; needs re-deploy with the v0.5 cohort
    72/72 numbers now baked into RESULTS.md)

CLEARED by the 2026-05-17 / 2026-05-18 session work:

  - §1.7 (all 4 sub-gates, including the llamacpp regression catch+fix)
  - §1.8 (RESULTS.md now points at v0.5 cohort)
  - §3 (cross-stack bench complete, 72/72/72)
  - §3.5 (bench surface coverage gate added + cleared)
  - §6 (deploy GREEN per `5e5ac5c`; verify post-§12 re-deploy)
  - §7 (engine forks all rebuilt + pushed; PRs filed + DCO-signed)
  - §11 (5 engine images live on Hub at :v0.5.0 + :latest)

### 9 · Tags

- [x] Annotated `v0.5.0` tag on Codec `main` (commit `3acffb9`).
- [x] Annotated `v0.5.0` tag on `codec-supervisor` `main` (commit
      `aeaa531`).
- [x] `git push origin v0.5.0` on both repos.

### 10 · Package publishes (workflow-triggered)

Triggered by the `v0.5.0` tag push on Codec; idempotent skip-if-version-exists per package.

- [x] `@codecai/web@0.5.0` (publish-npm success)
- [x] `@codecai/web-safety@0.5.0`
- [x] `@codecai/web-llm@0.5.0`
- [x] `@codecai/maps-cli@0.5.0`
- [x] `@codecai/mcp-leaf@0.5.0`
- [x] `@codecai/tool-kit@0.5.0`
- [x] `@codecai/wire-compress@0.5.0`
- [ ] `codecai@0.5.0` (PyPI) — workflow waiting on env approval
      (trusted-publisher OIDC gate). Same waiting-state pattern as
      v0.4.1; operator action: approve at
      `https://github.com/wdunn001/Codec/actions/runs/26014800184`.
- [x] `Codec.Net@0.5.0` (NuGet) — publish-nuget success
- [x] `codec-rs@0.5.0` (crates.io) — publish-cargo success
- [ ] ~~`ai.codec:codec:0.5.0` (Maven Central)~~ — publish-maven
      failed with Sonatype 401 Unauthorized
      (s01.oss.sonatype.org). Operator action: refresh
      `MAVEN_USERNAME` / `MAVEN_CENTRAL_TOKEN` secrets or migrate
      to the new Central Portal API at `central.sonatype.com`.
      Per Will's 2026-05-18 call: deferred, do not block release.
- [ ] `libcodec` vcpkg + FetchContent v0.5.0 docs (operator)

### 11 · Docker Hub publishes (operator)

- [x] `wdunn001/codec-sglang:v0.5.0` + `:latest`
- [x] `wdunn001/codec-vllm:v0.5.0` + `:latest`
- [x] `wdunn001/codec-llamacpp:v0.5.0` + `:latest` (rebuilt with
      dict-bake + check-dict-availability.sh per §1.7 fixes)
- [ ] ~~`wdunn001/codec-tgi:v0.5.0`~~ — DROPPED (see §7 above)
- [x] `wdunn001/codec-comfyui:v0.5.0` + `:latest`
- [x] `wdunn001/codec-diffusers:v0.5.0` + `:latest`
- [ ] `wdunn001/codec-metamcp:v0.5.0` — not yet built/pushed for v0.5
- [ ] `wdunn001/codec-time-leaf:v0.5.0` — not yet built/pushed for v0.5

### 12 · Website publish (operator)

- [x] codec-website main → v0.5 (commits `fd090ad` Hero/Benchmarks
      bump + new changelog entry; `3d5fd00` schema-enum fix). Pushed
      to `wdunn001/codec-website` `main`.
- [x] `ssh william@192.168.1.198 'cd /storage/codec-website && git
      pull && docker compose up -d --build'` — deploy GREEN; container
      recreated + started.
- [x] codecai.net serves v0.5 numbers — Hero now `v0.5.0 shipping`;
      `/changelog/` lists the new entry titled "v0.5.0 — efficiency,
      observability, and cohort honesty".

---

## Post-publish (operator)

- [ ] GitHub Release v0.5.0 with notes (template carries the
      "v0.4.1 → v0.5.0 is non-breaking; diff audit attached"
      conclusion)
- [ ] LinkedIn article corrected energy numbers (DONE in article
      `87f8842`; share/announcement pending)
- [ ] IETF I-D submitted (operator runs `kdrfc
      docs/submissions/ietf-draft-codec-00.md`, uploads to datatracker)
- [ ] OIP GitHub discussion opened
- [ ] Memory notes updated for cohort move

---

## Notes for this run

- Engine-fork code landed across all 4 forks; release-cut tags them
  at current HEAD.
- v0.4 OQ4 marked Resolved (`2f7ebfc` + `PROTOCOL_VERSION_HISTORY.md`
  update in this batch).
- 178 new tests added in the v0.5 work session, all green.
- ENERGY_METHODOLOGY.md math correction memo (`ab1b893`) caught
  the bug that would have shipped to website + LinkedIn — fixed
  pre-cut.
- v0.4.1 → v0.5.0 diff audit: NON-BREAKING. Wire-additive only.
- Pre-publish phase is partially green. Items 1.5 / 1.7 / 3 / 6 / 7
  blocked on operator action against shared infrastructure
  (RC engine image builds, lab-box deploy, fork tag cuts).
