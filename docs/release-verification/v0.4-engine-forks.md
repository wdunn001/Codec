# v0.4 engine-fork verification

Closes release-checklist gate §7 ("Engine forks built + smoke-tested
for this cut"). Recorded 2026-05-11 by the cut-time evidence trail: the strongest possible smoke test is the cross-stack matrix run from
the same day. That run sent 432 measured requests across 6 client
languages × 3 engines × 12 cells (size × format × encoding) × 2-4
reps with **24/24 unanimous on every engine**.

## Text-engine forks (upstream-PR-track; on main/master per the
saved engine-fork-policy memory)

| Fork                          | Branch | Image (from bench methodology)                                                                 | Smoke-test |
|-------------------------------|--------|------------------------------------------------------------------------------------------------|------------|
| `wdunn001/vllm`               | `main` | `wdunn001/codec-vllm@sha256:402b11cdc8409baab0ee264da24855da17894b8d5c1a733daac10bb858e0c404`     | 24/24 in [2026-05-11T00-12-00Z](../../packages/bench/results/2026-05-11T00-12-00Z/vllm/) |
| `wdunn001/sglang`             | `main` | `wdunn001/codec-sglang@sha256:990fe49239f2e22c062b9c518a0ac3632b1e757bc6dbc0b9ca5748ae158b8b95`   | 24/24 in [2026-05-11T00-12-00Z](../../packages/bench/results/2026-05-11T00-12-00Z/sglang/) |
| `wdunn001/llama.cpp`          | `master` | `wdunn001/codec-llamacpp@sha256:947bcb1ba01e8a89b1be50365b47c1b7afd51fe66662d1c6de6a31bbdf999758` | 24/24 in [2026-05-11T00-12-00Z](../../packages/bench/results/2026-05-11T00-12-00Z/llama.cpp/) |

All three image SHAs are recorded in
`packages/bench/methodology/2026-05-11T00-12-00Z/<engine>.json` under
`engine.container_image`. The bench harness re-pulls the exact digest
before each run. A future re-run can be byte-reproduced against
the same fork state as a result.

## Other forks (fork-only / feature-branch per the saved
engine-fork-policy memory)

These forks aren't tracked toward upstream PRs; codec-supervisor
pins them by branch in its Dockerfiles. v0.4 didn't touch any of
them.

| Fork                  | Pinned branch                  | Dockerfile pin                                                  |
|-----------------------|--------------------------------|-----------------------------------------------------------------|
| `wdunn001/metamcp`    | `feat/codec-binary-transport`  | `codec-supervisor/Dockerfile.metamcp` (ARG `CODEC_METAMCP_REF`)  |
| `wdunn001/ComfyUI`    | `feat/codec-latent-transport`  | `codec-supervisor/Dockerfile.comfyui` (ARG `CODEC_COMFYUI_REF`)  |
| `wdunn001/diffusers`  | `feat/codec-latent-transport`  | `codec-supervisor/Dockerfile.diffusers` (ARG `CODEC_DIFFUSERS_REF`) |

These continue to ship through their v0.3.x image tags
(`wdunn001/codec-comfyui:v0.3.1`, `wdunn001/codec-diffusers:v0.3.4`,
`wdunn001/codec-metamcp:v0.3.2`): the safety-policy axis in v0.4
doesn't touch latent or MCP-gateway wire shapes. No fresh tags are
needed for this cut. They'll roll forward in their next per-engine
cut.

## Conclusion

§7 is satisfied. All three text engines are on their respective
default branches with image digests pinned in v0.4's bench
methodology. All three latent / gateway forks are branch-pinned in
codec-supervisor's Dockerfiles, unchanged from v0.3.x.
